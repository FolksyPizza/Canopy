name: Canopy
main: com.folypizza.canopy.CanopyPlugin
version: '@project.version@'
api-version: '1.21'
author: anomaly
description: Distributed Folia shard coordination
load-order: AFTER
folia-supported: true

commands:
  region:
    description: Show the current region, list shards, or move between regions.
    usage: /region [list|go]

# Configuration is documented in config.yml.
